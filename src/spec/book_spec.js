goog.provide('gpub.spec.BookSpec');

/**
 * A book spec represents a serialized Book prototype. Re-running Gpub on the
 * same prototype should generate the same output everytime.
 *
 * The Book Spec is descendent from the Glift Spec, but has orthogonal concerns
 * and so is separate.
 *
 * @param {!gpub.BookSpec} options
 *
 * @constructor @final @struct
 */
gpub.spec.BookSpec = function(options) {

};
