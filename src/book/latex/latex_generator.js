gpub.book.latex.generator = function() {
  return new gpub.book.latex._Generator();
};

gpub.book.latex._Generator= function() {}

gpub.book.latex._Generator.prototype = {
  generate: function(spec, options) {
  },

  defaultTemplate: function() {
  },

  processBookOptions: function() {
  }
}
