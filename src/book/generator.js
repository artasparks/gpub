gpub.provide('gpub.book.Generator');

/**
 * Book generator helper.
 *
 * @struct @final @constructor
 */
gpub.book.Generator = function() {

};


// Some stubbed methods
gpub.book.Generator.prototype = {
  /**
   * Get the labels for an ID.
   * @param 
   */
  getLabels: function(id) {
  },

  hasLabel: function(id) {
  },

  forEachDiagram: function(fn) {
  }
};
