/**
 * Methods relating to extracting metadata.
 */
gpub.book.metadata = {
  /**
   * Processes the global metadata from the initial SGF.
   *
   * Returns: The processed comment
   */
  globalMetadata: function(movetree) {

  }
};
